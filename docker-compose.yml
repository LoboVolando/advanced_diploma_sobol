version: "3.7"
networks:
  skynet:
    driver: bridge
services:
  front:
    container_name: skill-front
    ports:
      - "5001:80"
    image: nginx:1.22.0
    volumes:
      - ./web/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./web/logs/nginx:/var/log/nginx
      - ./web/front/:/front